#include<bits/stdc++.h>
typedef struct node* nd ;
struct node{
    myclass as;
    nd next;
};
int main(){
    nd head;
    head=(nd)malloc(sizeof(struct node));
    nd Next;
    Next=(nd)malloc(sizeof(struct node);
    {
        /* data */
    };
    ))
    head->next=Next;
    {
        /* data */
    };
    ))

}